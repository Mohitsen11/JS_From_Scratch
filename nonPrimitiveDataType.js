// Object is one of non-primitive datatype

let employee = {
    name : "Alex",
    company: "Amazon",
    position: "Senior developer",
    salary: "$400k",
    employeeId: "Ax099"
}

// so rather than defining an real word entity data differently we use objects in place.

console.log(employee);

console.log(employee.company);